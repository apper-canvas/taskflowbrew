import HomePage from '@/components/pages/HomePage';
import NotFound from '@/pages/NotFound'; // Assuming NotFound remains in src/pages if it exists, otherwise moves too.
path: '/',
    element: HomePage,
    auth: false,
  },